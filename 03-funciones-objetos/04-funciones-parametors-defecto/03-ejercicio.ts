(()=>{
    function generarContrasena(longitud: number = 8, incluirNumeros: boolean = true, incluirMayusculas: boolean = true) {
        // Genera una contraseña aleatoria con las opciones especificadas.
        // ... (implementación de la lógica de generación de contraseñas)
        return contrasena;
      }
      
      let contrasena1 = generarContrasena(); // Contraseña de 8 caracteres, con números y mayúsculas
      let contrasena2 = generarContrasena(12, false, true); // Contraseña de 12 caracteres, solo mayúsculas
})();
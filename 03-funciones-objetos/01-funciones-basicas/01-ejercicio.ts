(()=>{
  // SumaDosNumeros.ts
function sumar(a: number, b: number): number {
    // Esta función suma dos números y devuelve el resultado.
    // Los parámetros 'a' y 'b' son de tipo número.
    // El tipo de retorno también es número.
    return a + b;
}
})();
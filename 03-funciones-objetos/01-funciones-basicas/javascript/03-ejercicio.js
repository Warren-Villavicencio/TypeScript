"use strict";
(() => {
    // EsPar.ts
    function esPar(numero) {
        // Esta función determina si un número es par.
        // Recibe un número como parámetro.
        // Devuelve un valor booleano (true si es par, false si no).
        return numero % 2 === 0;
    }
})();

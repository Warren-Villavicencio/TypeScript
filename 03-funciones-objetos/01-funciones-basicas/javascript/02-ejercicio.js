"use strict";
(() => {
    // SaludarPersona.ts
    function saludar(nombre) {
        // Esta función crea un saludo personalizado.
        // Recibe un nombre como parámetro de tipo cadena.
        // Devuelve un saludo con el nombre de la persona.
        return `¡Hola, ${nombre}!`;
    }
})();

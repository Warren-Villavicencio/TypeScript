"use strict";
(() => {
    // MultiplicarPorDos.ts
    const multiplicarPorDos = (numero) => numero * 2;
    // Esta es una forma más concisa de escribir una función.
    // Se utiliza mucho en TypeScript.
})();

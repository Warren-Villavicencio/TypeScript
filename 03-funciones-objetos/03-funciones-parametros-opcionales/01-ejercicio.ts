(()=>{
    function calcularAreaCirculo(radio: number, pi: number = 3.1416): number {
        // Calcula el área de un círculo dado su radio.
        // El valor de pi es opcional y por defecto se toma como 3.1416.
        return pi * radio * radio;
      }
})();